---@description "로그인 요청 구조체"
@Struct
script LoginRequest

	---@description "로그인 요청할 유저의 UID 목록"
	property table<string> uids = {}

	---@description "구조체를 테이블로 변환 합니다"
	method table ToTable()
		local content = {
			["uids"] = self.uids,
		}
		
		return content
	end

	---@description "테이블에서 구조체로 변환합니다"
	method LoginRequest FromTable(table content)
		if content == nil then
			return self
		end

		self.uids = content["uids"] or {}
		return self
	end

end