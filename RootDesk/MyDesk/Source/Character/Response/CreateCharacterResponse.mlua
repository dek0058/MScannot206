---@description "유저 캐릭터 생성 응답 구조체"
@Struct
script CreateCharacterResponse

	---@description "응답 리스트"
	property table<UserCreateCharacterResult> responses = {}

	---@description "테이블을 구조체로 변환 합니다"
	method CreateCharacterResponse FromTable(table content)
		if content == nil then
			return self
		end

		for _, r in ipairs(content["responses"] or {}) do
			local result = UserCreateCharacterResult():FromTable(r)
			table.insert(self.responses, result)
		end

		return self
	end

end