---@description "유저 캐릭터 삭제 응답 구조체"
@Struct
script DeleteCharacterResponse

	---@description "응답 리스트"
	property table<UserDeleteCharacterResult> responses = {}

	---@description "테이블을 구조체로 변환 합니다"
	method DeleteCharacterResponse FromTable(table content)
		if content == nil then
			return self
		end

		for _, r in ipairs(content["responses"] or {}) do
			local result = UserDeleteCharacterResult():FromTable(r)
			table.insert(self.responses, result)
		end

		return self
	end


end