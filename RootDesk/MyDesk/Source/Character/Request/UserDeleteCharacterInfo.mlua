---@description "유저 캐릭터 삭제 요청 정보"
@Struct
script UserDeleteCharacterInfo

	---@description "유저 고유 ID"
	property string uid = ""

	---@description "인증 토큰"
	property string token = ""

	---@description "삭제할 캐릭터 슬롯"
	property integer slot = 0

	---@description "구조체를 테이블로 변환 합니다"
	method table ToTable()
		local content = {
			["uid"] = self.uid,
			["token"] = self.token,
			["slot"] = self.slot
		}
		return content
	end

end